# Gateway Policy Contract

Use this folder for gateway-owned policy bundles, adapter code, and shared evaluation notes.

- `policy.rego` is the default starter.
- Add more policy modules here as the gateway grows.
- Keep inbound policy wiring in `config/gateway/policy-gate.yaml`.
