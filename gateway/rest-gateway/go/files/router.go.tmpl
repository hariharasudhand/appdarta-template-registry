package gateway

import "net/http"

// NewMux is the gateway assembly point.
// Keep concrete route registration in this file and place request/response
// shaping code under gateway/handlers/.
func NewMux() *http.ServeMux {
	mux := http.NewServeMux()
	return mux
}
