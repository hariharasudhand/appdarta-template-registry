"""Worker agents for {{ .ServiceName }}."""
from __future__ import annotations
from abc import ABC, abstractmethod
from typing import Any


class BaseWorker(ABC):
    """All workers implement this interface."""

    @abstractmethod
    async def execute(self, input: str, context: dict[str, Any] | None = None) -> dict[str, Any]:
        ...


class ResearchWorker(BaseWorker):
    """Performs information retrieval. Replace body with your retrieval logic."""

    async def execute(self, input: str, context: dict[str, Any] | None = None) -> dict[str, Any]:
        # TODO: integrate with your retriever / search tool
        return {"research": f"[stub] research results for: {input}"}


class SummaryWorker(BaseWorker):
    """Summarises research output. Replace body with your LLM call."""

    async def execute(self, input: str, context: dict[str, Any] | None = None) -> dict[str, Any]:
        research = (context or {}).get("research", {}).get("research", "")
        # TODO: call your LLM to summarise
        return {"summary": f"[stub] summary of: {research or input}"}
