# {{ .ProjectName }}

Generated by [darta-mint](https://appdarta.io) — {{ .ProjectName }} workspace.

## Services
{{ range .Agents }}
- **{{ .Name }}** — agent service
{{- end }}
{{ range .AllTanks }}
- **{{ .Name }}** — data tank
{{- end }}
{{ if .Gateway }}
- **{{ .Gateway.Name }}** — API gateway
{{ end }}

## Quick start

```bash
cp .env.example .env
# Fill in required values in .env
make dev
```

## Commands

| Command | Description |
|---------|-------------|
| `make dev` | Start full stack with docker compose |
| `make build` | Build all service images |
| `make test` | Run all service tests |
| `make down` | Stop all services |
| `make clean` | Stop and remove images + volumes |

## Deploy target

This workspace was scaffolded for: **{{ .DeployTarget }}**
