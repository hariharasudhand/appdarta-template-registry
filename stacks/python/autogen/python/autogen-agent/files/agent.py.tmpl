"""AutoGen agent skeleton for {{ .AgentName }} — structure only; domain logic via build prompts."""
from autogen import AssistantAgent

def build_agent():
    return AssistantAgent(
        name="{{ .AgentName }}",
        system_message="Structure-only scaffold. Replace with domain guidance from build/prompts.yaml.",
    )
