"""OpenAI Agents SDK skeleton for {{ .AgentName }} — structure only; domain logic via build prompts."""
from agents import Agent

def build_agent():
    return Agent(
        name="{{ .AgentName }}",
        instructions="Structure-only scaffold. Replace with domain guidance from build/prompts.yaml.",
    )
