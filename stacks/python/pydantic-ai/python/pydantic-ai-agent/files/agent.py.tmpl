"""PydanticAI agent skeleton for {{ .AgentName }} — structure only; domain logic via build prompts."""
from pydantic_ai import Agent

def build_agent():
    return Agent(
        "Structure-only scaffold. Replace with domain guidance from build/prompts.yaml.",
        system_prompt="Structure-only scaffold for {{ .AgentName }}.",
    )
