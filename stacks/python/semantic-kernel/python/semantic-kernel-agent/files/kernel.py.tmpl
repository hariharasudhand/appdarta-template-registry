"""Semantic Kernel skeleton for {{ .AgentName }}."""
from semantic_kernel import Kernel

def build_kernel() -> Kernel:
    kernel = Kernel()
    # Register plugins and planners here — domain substance from build/prompts.yaml.
    return kernel
