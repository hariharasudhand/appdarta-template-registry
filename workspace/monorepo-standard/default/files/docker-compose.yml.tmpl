{{ if eq .DeployTarget "docker-compose" -}}
# Generated by darta-mint — do not edit by hand.
# Re-generate with: darta-mint scaffold workspace monorepo-standard

include:
{{ range .Agents }}  - {{ .ComposeFragment }}
{{ end }}{{ range .AllTanks }}  - tanks/{{ .Name }}/docker-compose.yml
{{ end }}{{ if .Gateway.Name }}  - gateway/docker-compose.yml
{{ end }}{{ if .ProductUI.Framework }}  - ui/docker-compose.yml
{{ end }}
networks:
  default:
    name: {{ .ProjectName }}-net

{{ else if eq .DeployTarget "kubernetes" -}}
# Kubernetes deployment: see k8s/ directory for manifests.
# Run: kubectl apply -f k8s/

{{ else -}}
# docker-compose.yml not applicable for deploy target: {{ .DeployTarget }}
{{ end -}}
